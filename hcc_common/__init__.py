"""hcc_common — shared library for the HCC detection platform.

Provides a single source of truth for configuration, database models,
object storage, the Redis event bus, and the data-transfer schemas used
across every service (detector, reviewer, trainer, janitor).
"""

__version__ = "0.1.0"
